// Define the interface for a User entity
export interface IUsers {
    // Unique identifier for the user
    id: number,
    // Username chosen by the user
    user: string,
    // Password for the user's account
    password: string
}